import theme from 'styled-theming';

export const backgroundColor: theme.ThemeSet = theme('mode', {
  light: '#fff',
  dark: '#222831',
});

export const primaryTextColor: theme.ThemeSet = theme('mode', {
  white: '#222831',
  dark: '#eeeeee',
});
